{% extends 'base.html.twig' %}
 {% block stylesheets %}
     <style>
         .main{
             margin-top: 100px;
         }
     </style>
 {% endblock %}
{% block body %}
    <div class="container main">
        <div class="row align-items-center justify-content-center text-center p-5 text-dark fs-3">

            <div class="col-lg-6">
                <div class="card w-100 p-5">
                    <a href="{{ absolute_url(path('app_invoice_create')) }}">Add Invoice </a>
                </div>
            </div>
            <div class="col-lg-6">
                <div class="card w-100 p-5"><a href="{{ absolute_url(path('app_invoice_line_create')) }}">Add Invoice fields</a></div>
            </div>
            <div class="col-lg-12 mt-3">
                <div class="card w-100 p-5">
                    <a href="{{ absolute_url(path('app_invoice')) }}">List Invoice</a>
                </div>
            </div>

        </div>

    </div>

    {# <div class="container d-flex align-items-center justify-content-center text-center p-5 text-dark fs-3 gx-5 m-5">
        <div class="row text-center p-5 text-dark fs-3 gx-5 m-5">
            <div class="col-lg-6">
              <div class="card">
                  <a href="{{ absolute_url(path('app_invoice_create')) }}">List Invoice</a>
              </div>
            </div>
            <div class="col-lg-6">
                <div class="card">
                    <a href="{{ absolute_url(path('app_invoice_create')) }}">Add Invoice</a>
                </div>
            </div>

            <div class="col-lg-6">
                <div class="card">
                    <a href="{{ absolute_url(path('app_invoice_line_add')) }}">Add Invoice Line</a>
                </div>
            </div>
            <div class="col-lg-6">
                <div class="card"><a href="{{ absolute_url(path('app_invoice_line_add')) }}">Add Invoice Line</a></div>
            </div>
        </div>


    </div> #}
{% endblock %}

{% block title %}

{% endblock %}

